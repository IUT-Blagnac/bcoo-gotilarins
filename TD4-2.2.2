@startuml
actor Client
actor Vendeur
usecase esArt as (essayer_un_article)
usecase demRens as (demander_un_renseignement)
usecase pay as (payer)
usecase payCart as (payer_par_carte)
usecase payEsp as (payer_par_espèce)
usecase reduc as (reduction)
usecase in as (rentrer)
usecase go as (passer_dans_un_rayon)


Client -> esArt
Client --> demRens
Client --> in
Client --> go
demRens --> Vendeur
Client --> pay

reduc <- payEsp
payCart ->  reduc
payCart ..> "<<extends>>" pay
payEsp  ..> "<<extends>>" pay


@enduml
